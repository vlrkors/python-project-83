services:
  - type: web
    name: vlrkors_python-project-83
    env: python
    plan: free
    buildCommand: make build
    startCommand: make render-start
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: vlrkors_python-project-83-db
          property: connectionString

  - type: postgres
    name: vlrkors_python-project-83-db
    plan: free
    # Optional: restrict external access
    ipAllowList: []
